# Online Python compiler (interpreter) to run Python online.
# Write Python 3 code in this online editor and run it.
import random
import math

strana = (math.ceil(random.random()*2))
#strana = print (random.randint())

if strana > 1:
    print ("panna")
else:
    print ("orel")